{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" =~ \\.tsx?$ ]] && npx tsc --noEmit 2>&1 | tail -20; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Type-checking TypeScript..."
          }
        ]
      }
    ]
  }
}
